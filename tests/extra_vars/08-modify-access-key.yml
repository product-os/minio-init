---
minio_access_keys:
  - name: "test-service-account"
    associated_user: "testuser"
    secret_key: "{{ lookup('ansible.builtin.password', '/dev/null', length=40, chars=['ascii_letters', 'digits']) }}"
    expiry_duration: "24h"
    description: "Updated access key for testuser"
